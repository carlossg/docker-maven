name: amazoncorretto-11

on:
  push:
    paths:
      - "amazoncorretto-11/**"
      - .github/workflows/amazoncorretto-11.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"
  pull_request:
    paths:
      - "amazoncorretto-11/**"
      - .github/workflows/amazoncorretto-11.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"

jobs:
  build:
    uses: ./.github/workflows/_template.yml
    with:
      directory: amazoncorretto-11
    secrets: inherit
