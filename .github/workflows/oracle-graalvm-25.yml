name: oracle-graalvm-25

on:
  push:
    branches:
    - main
    paths:
      - "oracle-graalvm-25/**"
      - .github/workflows/oracle-graalvm-25.yml
      - .github/workflows/_template.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"
  pull_request:
    paths:
      - "oracle-graalvm-25/**"
      - .github/workflows/oracle-graalvm-25.yml
      - .github/workflows/_template.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"

jobs:
  build:
    uses: ./.github/workflows/_template.yml
    with:
      directory: oracle-graalvm-25
    secrets: inherit
