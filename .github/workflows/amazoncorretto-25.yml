name: amazoncorretto-25

on:
  push:
    branches:
    - main
    paths:
      - "amazoncorretto-25/**"
      - .github/workflows/amazoncorretto-25.yml
      - .github/workflows/_template.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"
  pull_request:
    paths:
      - "amazoncorretto-25/**"
      - .github/workflows/amazoncorretto-25.yml
      - .github/workflows/_template.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"

jobs:
  build:
    uses: ./.github/workflows/_template.yml
    with:
      directory: amazoncorretto-25
    secrets: inherit
