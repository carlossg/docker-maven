name: amazoncorretto-20

on:
  push:
    branches:
    - master
    paths:
      - "amazoncorretto-20/**"
      - .github/workflows/amazoncorretto-20.yml
      - .github/workflows/_template.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"
  pull_request:
    paths:
      - "amazoncorretto-20/**"
      - .github/workflows/amazoncorretto-20.yml
      - .github/workflows/_template.yml
      - common.sh
      - tags-for-dir.sh
      - "tests/**"
      - "!tests/*.ps*"
      - "!tests/*.windows"

jobs:
  build:
    uses: ./.github/workflows/_template.yml
    with:
      directory: amazoncorretto-20
    secrets: inherit
